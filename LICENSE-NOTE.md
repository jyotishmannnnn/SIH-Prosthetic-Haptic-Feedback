# License status

No license has been chosen for this repository yet. This file is a
placeholder so the decision isn't silently skipped.

**Recommendation (not applied):** MIT or Apache-2.0 for the firmware/PC
code in this repo (`firmware/`, `pc/`, `gui/`) — both are permissive and
standard for hackathon/prototype code. Apache-2.0 additionally includes an
explicit patent grant, which may matter if this becomes a real product.

Note: `docs/` references the upstream **eFlesh** project
(https://github.com/notloop/eFlesh or equivalent — see `docs/hardware.md`
for the exact source used) for MLX90393 sensor configuration. That
upstream project has its own license; nothing from its dataset/CAD/source
tree is copied into this repository, so its license does not need to be
reconciled here. If upstream code is ever vendored in directly, check its
license compatibility first.

Once you've decided, replace this file with a real `LICENSE` file and
remove this note.
